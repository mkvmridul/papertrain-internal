# Prior work: what existed before 09:00 on 18 September 2026

The finalist briefing asks teams to say what existed before build day and what was built during the event. This file is that declaration. It was committed before build day so the timestamp supports it; the git history shows the same.

Everything listed below was written between 17 September and the morning of 18 September, and was verified against a **local** Elasticsearch 9.5.4 + Kibana 9.5.4 running in Docker on a laptop. Nothing had been connected to an Elastic Cloud project, to Amazon Bedrock, or to Sarvam before build day. The agent had never run against a language model before build day, because no model was available locally. The build was done with AI pair-programming assistance (Claude Code), with every step run and checked by the team.

## Existed before build day

| Path | What it is | Verified before build day by |
|---|---|---|
| `src/generate.js` | Deterministic synthetic data generator and answer key | `npm test` (14 tests), `npm run generate` |
| `data/*.ndjson`, `data/answer-key.json` | The generated dataset | Regenerated byte for byte from the seed |
| `elastic/mappings/*.json` | Four index mappings with `_meta` notes | `npm run ingest` against local ES 9.5.4 |
| `src/ingest.js` | Index creation and bulk load | Same |
| `elastic/tools/*.esql`, `elastic/tools/tools.json` | The five ES|QL tools and their Agent Builder definitions | `npm run verify`: all checks passed on local ES 9.5.4 |
| `src/verify.js` | Scores the tools against the answer key, writes `BENCHMARKS.md` | Run locally; output committed |
| `elastic/agent/instructions.md` | Agent instructions | Read only; not yet exercised by a model |
| `elastic/workflows/ledgerlens-open-case.yaml` | The open-case workflow | Created and executed on local Kibana 9.5.4: Kibana case created, audit record written |
| `src/setup-agent.js` | Creates workflow, tools, agent via the Kibana API; smoke-executes one tool | Run locally, re-run to confirm idempotent updates |
| `src/console/server.js`, `src/console/index.html`, `src/console/traceability.js` | The ops console and the traceability checker | Exercised end to end in `AGENT_MODE=mock` (no model) including approve → workflow → audit record; checker unit-tested |
| `src/es.js` | HTTP helpers | Used by everything above |
| `tests/*.test.js` | Unit tests | `npm test` |
| `presentation/LedgerLens.pptx`, `presentation/build-deck.js` | The deck and the script that builds it | Structural validation; rendered through Keynote for a visual check |
| `README.md`, `DATA.md`, `DEMO.md`, `BENCHMARKS.md`, this file | Documentation | Read |

## Not done before build day (to be built or completed on 18 September)

- Connecting to the Elastic Cloud Serverless project handed out for the finale; ingest and verify there; `BENCHMARKS.md` regenerated from that project.
- First run of the agent against a real model, and any tuning of `elastic/agent/instructions.md` that follows.
- Amazon Bedrock inference endpoint on the AWS sandbox issued on the day, and confirming the console converses through it.
- Sarvam translation with the key issued on the day.
- Recording the demo video and committing it.
- Final README numbers and screenshots.

## Build-day log

Filled in during the 18th, with times.

| Time | What changed |
|---|---|
| 02:00 | Fixed `src/generate.js`: commit 9caa5df had added the rail-based amount block and the `k < 20` loop without removing the lines they replaced, so `npm run generate` and the generator tests failed with a SyntaxError. Removed the six stale lines; regenerated data is byte-identical to the committed files; 14/14 tests. |
| 08:00 | One action per break type. Four new Elastic Workflows beside `ledgerlens-open-case` (`raise-reversal`, `log-fee-dispute`, `close-timing`, `escalate-partner`), listed in `elastic/workflows/actions.json` and registered as Agent Builder workflow tools by `setup-agent.js`. Agent instructions gain the action table and a **Draft message** section. Audit mapping extended (still strict). `tests/actions.test.js`. Not yet run against Kibana at the time of writing. |
| 08:00 | Console: `GET /api/break` prefetches `ledgerlens.break_delta` on click and paints the ES|QL verdict before the model starts (display only; traceability unchanged). Approve button names the action; audit line shows what ran. `POST /api/act` replaces `/api/open-case` for the direct fallback and picks the workflow from the break type. |
